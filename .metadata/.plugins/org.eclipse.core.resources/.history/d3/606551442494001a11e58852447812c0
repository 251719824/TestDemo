spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/yishujiushipaidaxingzz/sp-config
          search-paths: config
            
